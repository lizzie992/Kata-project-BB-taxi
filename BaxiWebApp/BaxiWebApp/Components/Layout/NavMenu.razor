@implements IDisposable
@inject NavigationManager NavigationManager
@using Pages
@using BB
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Identity
@using Microsoft.EntityFrameworkCore
@using Microsoft.Extensions.Localization
@using System.Globalization
@using BaxiWebApp.Data
@inject AuthenticationStateProvider AuthStateProvider;
@inject SignInManager<User> signInManger;
@inject UserManager<User> UserManager;
@inject Data.BaxiWebAppContext context;
@using Pages.Res;
@inject IStringLocalizer<Resources> localizer
@inject Data.DataService DataService
@inject IDbContextFactory<BaxiWebAppContext> DbContextFactory
@inherits LayoutComponentBase

<style>

	.navbar-toggler {
		font-family: 'Merriweather', serif;
		background-color: #0066B2;
		color: #FFFFFF;
	}

</style>


@* <div class="top-row ps-3 navbar navbar-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="">Baxi</a>
	</div>
</div> *@

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
	<nav class="flex-column">
		<div class="nav-item px-3">
			<NavLink class="nav-link" href="" Match="NavLinkMatch.All">
				<span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> @localizer["Home"]
			</NavLink>
		</div>

		<AuthorizeView>
			<Authorized>
				<div class="nav-item px-3">
					<NavLink class="nav-link" href="Chat" D>
						<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["Chat"]
					</NavLink>
				</div>
				@if (currentlyLoggedInUser is not null && currentlyLoggedInUser.isActive == true)
				{
					<div class="nav-item px-3">
						<NavLink class="nav-link" href="ads">
							<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["All_ads"]
						</NavLink>
					</div>
					<div class="nav-item px-3">
						<NavLink class="nav-link" href="MyAdsForRecommendations">
							<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["Recommend_me_ads"]
						</NavLink>
					</div>

				}
				<div class="nav-item px-3">
					<NavLink class="nav-link" href="MyAds">
						<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["My_ads"]
					</NavLink>
				</div>
				@if (currentlyLoggedInUser is not null && currentlyLoggedInUser.isActive == true)
				{
					<div class="nav-item px-3">
						<NavLink class="nav-link" href="CreateAd">
							<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["Create_an_ad"]
						</NavLink>
					</div>
				}
				<div class="nav-item px-3">
					<NavLink class="nav-link" href="profile">
						<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["Profile"]
					</NavLink>
				</div>
				@if (currentlyLoggedInUser is not null && currentlyLoggedInUser.isActive == true)
				{
					<AuthorizeView Roles="Admin" Context="authContext">
						<div class="nav-item px-3">
							<NavLink class="nav-link" href="UserManagement">
								<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["User_management_page"]
							</NavLink>
						</div>
					</AuthorizeView>
				}

				<div class="nav-item px-3">
					<form action="Account/Logout" method="post">
						<AntiforgeryToken />
						<input type="hidden" name="ReturnUrl" value="Account/Login" />
						<button type="submit" class="nav-link">
							<span class="bi bi-arrow-bar-left-nav-menu" aria-hidden="true"></span> @localizer["Logout"]
						</button>
					</form>
				</div>
			</Authorized>

			<NotAuthorized>
				<div class="nav-item px-3">
					<NavLink class="nav-link" href="Account/Register">
						<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["Register"]
					</NavLink>
				</div>

				<div class="nav-item px-3">
					<NavLink class="nav-link" href="Account/Login">
						<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @localizer["Log_in"]
					</NavLink>
				</div>

			</NotAuthorized>
		</AuthorizeView>
	</nav>
</div>

@code {

	private string? currentUrl;
	protected override void OnInitialized()
	{
		DataService.CultureChanged += OnCultureChanged;
		currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
		NavigationManager.LocationChanged += OnLocationChanged;
	}

	private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
	{
		currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
		StateHasChanged();
	}

	public void Dispose()
	{
		NavigationManager.LocationChanged -= OnLocationChanged;
	}

	// protected override async Task OnParametersSetAsync()  //use async variant
	// {
	// 	await identifyLoggedInUser();
	// 	base.OnParametersSet();
	// }

	[CascadingParameter(Name = "currentlyLoggedInUser")]
	private User? currentlyLoggedInUser { get; set; }


	// public User? currentlyLoggedInUser;
	// public async Task identifyLoggedInUser()
	// {
	// 	using var context = DbContextFactory.CreateDbContext();
	// 	{
	// 		var authState = await AuthStateProvider.GetAuthenticationStateAsync();

	// 		var claims = authState.User;

	// 		currentlyLoggedInUser = await UserManager.GetUserAsync(claims);
	// 	}
	// }


	public async void OnCultureChanged(object? sender, CultureInfo culture)
	{
		await InvokeAsync(() =>
	{
		StateHasChanged();
	});
	}

	protected override void OnAfterRender(bool firstrender)
	{
		if (firstrender)
		{
			DataService.loadCulture(currentlyLoggedInUser);
		}
	}
}